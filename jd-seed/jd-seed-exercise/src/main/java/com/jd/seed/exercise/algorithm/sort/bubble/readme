交换排序-冒泡排序

基本思想：

在要排序的一组数中，对当前还未排好序的范围内的全部数，自上而下对相邻的两个数依次进行比较和调整，让较大的数往下沉，较小的往上冒。
即：每当两相邻的数比较后发现它们的排序与排序要求相反时，就将它们互换。
关键点：
1、相邻比较，相邻互换
2、遇大数后移，每轮循环将最大数放在length-firstlevel-1位置
3、

复杂度：av->O(n^2),max->O(n^2)
